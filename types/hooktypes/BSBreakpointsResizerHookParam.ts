export interface BSBreakpointsResizerHookParams {
  initialWidth: number;
  initialHeight: number;
  xs: [number, number];
  sm: [number, number];
  md: [number, number];
  lg: [number, number];
  xl: [number, number];
  xxl: [number, number];
}
